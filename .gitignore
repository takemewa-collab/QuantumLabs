.quantumlabs/

# local env (secrets / pod URL) — .env.example is tracked
.env

# web frontend (v0.5.1-b)
web/node_modules
web/.next

# eval kosu artefaktlari (raporlar yerel; trend icin istenirse commit edilir)
eval/reports/

# uretilen egitim verisi (transcript+feedback -> SFT)
train/data/
