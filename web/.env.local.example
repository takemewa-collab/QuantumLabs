# API base URL the frontend talks to (dev default: http://localhost:8000).
NEXT_PUBLIC_API_URL=http://localhost:8000

# Optional: set to require a Bearer token on the API (must match the API's API_KEY env).
# Leave empty for local dev (no auth).
NEXT_PUBLIC_API_KEY=
